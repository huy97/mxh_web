import React from 'react';

const SplashScreen = props => {
    return (
        <div className="splash-screen">
            <div className="splash-screen__loading"></div>
        </div>
    );
};

export default SplashScreen;